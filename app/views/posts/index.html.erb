<h1>みんなの投稿</h1></h1>

<p>フリーワードで探す</p>
<p>施設から探す</p>
<p>都道府県から探す（複数選択可）</p>
<p>こだわり検索</p>

<% @posts.each do |post| %>
  <div class="card w-50  mx-auto mt-4 shadow-lg">
    <%= link_to post_path(post.id) do %>
      <%= image_tag post.get_image, class: "card-img-top"; %>
    <% end %>
    <div class="card-body">
      <div class="row">
        <div class="col">
          <p class="card-title">
            <%= link_to post_path(post.id), class: "text-dark" do %>
              <strong><i class="far fa-flag"></i><span> <%= post.title %></span></strong>
            <% end %>
          </p>
          <p class="card-text">
            <%= post.shop_name %><br>
            <%= post.prefecture %>
          </p>
          <span><i class="fas fa-user"></i> By <%= post.user.name %> ｜ </span>
          <span><%= link_to "#{post.comments.count} コメント", post_path(post.id) %></p>    
        </div>
      </div>
    </div>
  </div>
<% end %>